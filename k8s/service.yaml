apiVersion: v1
kind: Service
metadata:
  name: poker-app-service
  namespace: poker-system
spec:
  type: NodePort
  selector:
    app: poker-app
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30501
